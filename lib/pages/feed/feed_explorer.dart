import 'package:flutter/material.dart';

class FeedExplorer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('Feed Explorer'),
      ),
    );
  }
}
